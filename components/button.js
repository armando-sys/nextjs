export default function button({value, background, color, padding,border
    return (
        <button style= {{
            background: background,
            color: color,
            padding: padding,
            border: border,
        }}
        >
        </button>
    );
}
    